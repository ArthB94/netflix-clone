apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: frontend
spec:
  hosts:
    - "*"
  http:
    - match:
        - uri:
            prefix: "/api/auth"
      route:
        - destination:
            host: auth-service
            port:
              number: 5000
    - match:
        - uri:
            prefix: "/api/movies"
      route:
        - destination:
            host: movies-service
            port:
              number: 5001
